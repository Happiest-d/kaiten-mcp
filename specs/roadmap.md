# Kaiten MCP Server — Roadmap

**Цель:** Полноценная работа с задачами Kaiten через MCP для AI-агентов.

## Текущий статус (4 фичи реализовано, 17 всего)

✅ **get-task-details** — получение детальной информации о задаче с комментариями
✅ **get-time-logs** — получение логов учёта времени
✅ **get-task-status** — получение статуса задачи (batch до 50)
✅ **create-task** — создание новой задачи

🔥 **Следующая критичная фича:** get-board-cards — получение списка задач на доске по колонкам

---

## Приоритеты фич

### 🔴 Приоритет 1 (критично для базовой работы)

**Фичи для управления жизненным циклом задачи:**

#### 5. **update-task** — обновление задачи
**Зачем:** Изменение title, description, перемещение между колонками/лейнами, изменение участников и тегов.

**API endpoint:** `PATCH /cards/{card_id}`

**Параметры:**
- `card_id` (обязательный)
- `title` (опционально)
- `description` (опционально)
- `column_id` (опционально) — перемещение между колонками
- `lane_id` (опционально) — перемещение между лейнами
- `owner_id` (опционально)
- `members` (опционально) — массив user_id
- `tags` (опционально) — массив tag_id

**Сценарии:**
- Переименовать задачу
- Обновить описание
- Переместить задачу в другую колонку (например, "В работе" → "Готово")
- Переназначить исполнителя
- Добавить/удалить участников
- Изменить теги

---

#### 5A. **get-board-cards** — получение списка задач на доске
**Зачем:** 🔥 **КРИТИЧНО** — видеть все задачи на доске, фильтровать по колонкам/лейнам, искать задачи без знания card_id. Базовая операция для работы с досками.

**API endpoint:** `GET /cards`

**Параметры фильтрации:**
- `board_id` (рекомендуемый) — фильтр по доске
- `column_id` (опционально) — фильтр по конкретной колонке
- `lane_id` (опционально) — фильтр по лейну (дорожке)
- `space_id` (опционально) — фильтр по пространству
- `condition` (опционально) — 1 = активные (on board), 2 = архивные
- `type_id` (опционально) — фильтр по типу карточки

**Параметры поиска:**
- `query` (опционально) — текстовый поиск по названию задачи
- `created_before` (опционально) — созданные до даты (ISO 8601)
- `created_after` (опционально) — созданные после даты (ISO 8601)
- `updated_before` (опционально) — обновлённые до даты (ISO 8601)
- `updated_after` (опционально) — обновлённые после даты (ISO 8601)

**Параметры пагинации:**
- `limit` (опционально) — количество карточек (по умолчанию 50, максимум 100)
- `skip` (опционально) — количество карточек для пропуска (offset)

**Формат ответа:**
Массив карточек с полями:
```json
[
  {
    "card_id": 12345,
    "title": "Название задачи",
    "board_id": 1660008,
    "column_id": 5747562,
    "lane_id": null,
    "state": "active",
    "owner_id": 501,
    "members": [...],
    "tags": [...],
    "created_at": "2026-02-01T10:00:00Z",
    "updated_at": "2026-02-10T14:30:00Z"
  },
  ...
]
```

**Сценарии:**
- **Получить ВСЕ задачи на доске** → `{ board_id: 1660008, limit: 100 }`
- **Задачи конкретной колонки** → `{ board_id: 1660008, column_id: 5747562 }`
- **Активные задачи (без архива)** → `{ board_id: 1660008, condition: 1 }`
- **Поиск по названию** → `{ board_id: 1660008, query: "баг авторизации" }`
- **Недавно обновлённые** → `{ board_id: 1660008, updated_after: "2026-02-10T00:00:00Z" }`
- **Пагинация больших досок** → `{ board_id: 1660008, limit: 50, skip: 0 }` → следующая страница: `skip: 50`

**Практические советы:**
- Всегда указывайте `board_id` для фильтрации — без него API вернёт задачи со всех досок
- Для больших досок (>100 задач) используйте пагинацию (`limit` + `skip`)
- `condition: 1` исключает архивные задачи (рекомендуется по умолчанию)
- Комбинируйте фильтры: `board_id` + `column_id` + `query` для точного поиска

**Важно:** Эта фича критична для навигации по доске и должна быть реализована сразу после `update-task`.

---

#### 6. **get-board-structure** — получение структуры доски
**Зачем:** Узнать ID и названия колонок, лейнов, доступных тегов — необходимо для правильного использования `get-board-cards`, `create-task` и `update-task`.

**API endpoints:**
- `GET /boards/{board_id}` — информация о доске

**Параметры:**
- `board_id` (обязательный)

**Формат ответа:**
```json
{
  "board_id": 1660008,
  "title": "Разработка",
  "columns": [
    {
      "column_id": 5747562,
      "title": "Бэклог",
      "sort_order": 0,
      "type": "normal",
      "wip_limit": null
    },
    {
      "column_id": 5747563,
      "title": "В работе",
      "sort_order": 1,
      "type": "active"
    }
  ],
  "lanes": [
    {
      "lane_id": 1001,
      "title": "Высокий приоритет",
      "sort_order": 0
    }
  ],
  "tags": [
    { "tag_id": 1, "name": "bug" },
    { "tag_id": 2, "name": "feature" }
  ]
}
```

**Сценарии:**
- Узнать ID колонки "В работе" перед перемещением задачи
- Получить список доступных тегов для создания задачи
- Узнать структуру новой доски перед началом работы
- Валидация column_id/lane_id перед вызовом `update-task`

**Важно:** Эта фича необходима для корректной работы с `get-board-cards` (чтобы знать column_id для фильтрации).

---

#### 7. **delete-task** — удаление задачи
**Зачем:** Удаление дубликатов, ошибочно созданных задач или устаревших карточек.

**API endpoint:** `DELETE /cards/{card_id}`

**Параметры:**
- `card_id` (обязательный)

**Сценарии:**
- Удалить дубликат задачи
- Удалить ошибочно созданную задачу
- Очистка старых тестовых задач

**Важно:** Обычно в Kaiten есть архивация — стоит добавить возможность архивирования вместо полного удаления.

---

#### 8. **add-comment** — добавление комментария к задаче
**Зачем:** AI-агент может документировать свою работу, задавать вопросы или оставлять статус-апдейты.

**API endpoint:** `POST /cards/{card_id}/comments`

**Параметры:**
- `card_id` (обязательный)
- `text` (обязательный) — текст комментария (до 4096 символов)
- `mentions` (опционально) — массив user_id для упоминаний

**Сценарии:**
- Оставить комментарий с результатами работы
- Задать вопрос исполнителю
- Зафиксировать принятое решение

---

#### 9. **log-time** — добавление записи учёта времени
**Зачем:** AI-агент может логировать затраченное время на выполнение задачи.

**API endpoint:** `POST /cards/{card_id}/time-logs`

**Параметры:**
- `card_id` (обязательный)
- `time_spent` (обязательный) — время в минутах
- `for_date` (обязательный) — дата в формате YYYY-MM-DD
- `comment` (опционально) — комментарий к записи
- `user_id` (опционально) — по умолчанию текущий пользователь

**Сценарии:**
- Залогировать время работы над задачей
- Фиксировать затраты времени для отчётности

---

### 🟡 Приоритет 2 (важно для продуктивной работы)

#### 10. **attach-file** — прикрепление файла к задаче
**Зачем:** AI-агент может прикладывать скриншоты, логи, результаты анализа к задаче.

**API endpoint:** `POST /cards/{card_id}/files`

**Параметры:**
- `card_id` (обязательный)
- `file` (обязательный) — бинарные данные файла
- `filename` (обязательный) — имя файла
- `content_type` (опционально) — MIME-type

**Сценарии:**
- Прикрепить скриншот ошибки
- Прикрепить файл с результатами тестирования
- Загрузить документацию

**Важно:** Нужно продумать работу с бинарными данными в MCP (base64 encoding или ссылки на локальные файлы).

---

#### 11. **manage-checklist** — работа с чек-листами
**Зачем:** Создание и обновление чек-листов в задачах для декомпозиции работы.

**API endpoints:**
- `POST /cards/{card_id}/checklists` — создать чек-лист
- `POST /checklists/{checklist_id}/items` — добавить пункт
- `PATCH /checklist-items/{item_id}` — отметить выполненным
- `DELETE /checklist-items/{item_id}` — удалить пункт

**Параметры (создание чек-листа):**
- `card_id` (обязательный)
- `name` (обязательный) — название чек-листа
- `items` (опционально) — массив строк (пункты чек-листа)

**Сценарии:**
- Создать чек-лист "Шаги для релиза"
- Отметить выполненный пункт
- Добавить новый пункт в существующий чек-лист

---

### 🟢 Приоритет 3 (полезные дополнения)

#### 12. **get-user-info** — получение информации о пользователях
**Зачем:** Узнать ID пользователя по имени для назначения исполнителя.

**API endpoints:**
- `GET /users/{user_id}` — информация о пользователе
- `GET /users` — список пользователей

**Сценарии:**
- Найти user_id по имени "Иван Иванов"
- Получить список всех участников проекта

---

#### 13. **manage-tags** — управление тегами
**Зачем:** Создание новых тегов для классификации задач.

**API endpoints:**
- `GET /boards/{board_id}/tags` — список тегов доски
- `POST /boards/{board_id}/tags` — создать тег
- `PATCH /tags/{tag_id}` — обновить тег
- `DELETE /tags/{tag_id}` — удалить тег

**Сценарии:**
- Создать новый тег "ai-generated"
- Переименовать тег
- Получить список всех тегов доски

---

#### 14. **get-custom-fields** — работа с кастомными полями
**Зачем:** Чтение и запись значений кастомных полей (например, "Приоритет", "Оценка времени").

**API endpoints:**
- `GET /cards/{card_id}/properties` — получить значения полей
- `PATCH /cards/{card_id}/properties` — установить значения

**Параметры:**
- `card_id` (обязательный)
- `properties` (объект) — ключ-значение кастомных полей

**Сценарии:**
- Установить приоритет задачи
- Указать оценку времени на выполнение

---

#### 15. **archive-task** — архивирование задачи
**Зачем:** Переместить задачу в архив вместо удаления.

**API endpoint:** `PATCH /cards/{card_id}` с `state: archived`

**Параметры:**
- `card_id` (обязательный)

**Сценарии:**
- Архивировать завершённую задачу
- Скрыть неактуальную задачу без удаления

---

#### 16. **bulk-operations** — массовые операции
**Зачем:** Обновление нескольких задач за один запрос.

**API endpoints:**
- `PATCH /cards/bulk` — массовое обновление
- `POST /cards/bulk` — массовое создание

**Параметры:**
- `cards` (массив) — список задач с параметрами для обновления/создания

**Сценарии:**
- Переместить все задачи с тегом "bug" в колонку "Backlog"
- Создать серию связанных задач

---

#### 17. **link-cards** — связывание задач
**Зачем:** Установить связи между задачами (блокировка, зависимость, дубликат).

**API endpoint:** `POST /cards/{card_id}/relations`

**Параметры:**
- `card_id` (обязательный)
- `target_card_id` (обязательный)
- `relation_type` (обязательный) — тип связи: "blocks", "depends_on", "duplicates", "relates_to"

**Сценарии:**
- Указать, что задача A блокирует задачу B
- Отметить задачу как дубликат другой

---

## Метрики полноты

| Категория | Фичи | Статус |
|-----------|------|--------|
| **Чтение данных** | get-task-details, get-time-logs, get-task-status, get-board-cards, get-board-structure, get-user-info | 3/6 ✅ |
| **Создание** | create-task, add-comment, log-time, attach-file, manage-checklist, manage-tags | 1/6 ✅ |
| **Обновление** | update-task, manage-checklist, get-custom-fields | 0/3 ❌ |
| **Удаление** | delete-task, archive-task | 0/2 ❌ |
| **Массовые операции** | bulk-operations, link-cards | 0/2 ❌ |

---

## План реализации (рекомендуемый порядок)

### Этап 1: Базовое редактирование и навигация (Приоритет 1)
1. ✅ ~~get-task-details~~ — **готово**
2. ✅ ~~get-task-status~~ — **готово**
3. ✅ ~~create-task~~ — **готово**
4. ✅ ~~get-time-logs~~ — **готово**
5. **update-task** — редактирование задач
6. **get-board-cards** 🔥 — список задач на доске (КРИТИЧНО)
7. **get-board-structure** — структура доски (колонки, лейны, теги)
8. **delete-task / archive-task**
9. **add-comment**
10. **log-time**

### Этап 2: Расширенные возможности (Приоритет 2-3)
11. **attach-file**
12. **manage-checklist**
13. **manage-tags**
14. **get-user-info**
15. **get-custom-fields**

### Этап 3: Продвинутые фичи (Приоритет 3)
16. **bulk-operations**
17. **link-cards**

---

## Источники информации

- [Kaiten API Documentation](https://developers.kaiten.ru/) — официальная документация API
- [Kaiten API FAQ](https://faq.kaiten.io/docs/api) — детальное описание эндпоинтов
- [Kaiten FAQ](https://faq.kaiten.io/space/2502/card/41560) — база знаний
- [GitHub - Xitroy/Kaiten Python Client](https://github.com/Xitroy/Kaiten) — Python клиент с примерами
- [GitHub - ksysoev/kaiten-api-client-python](https://github.com/ksysoev/kaiten-api-client-python) — альтернативный Python клиент
- [Kaiten API - Columns, lanes, cells](https://faq-en.kaiten.site/192045c2-27e7-4b5e-8e43-f48fb5b50b61) — структура досок

---

## Примечания

1. **Авторизация**: Все эндпоинты требуют Bearer token из `KAITEN_API_TOKEN`.
2. **Rate limits**: Уточнить лимиты в официальной документации Kaiten.
3. **Webhook support**: Возможно, в будущем добавить поддержку вебхуков для получения событий.
4. **Пагинация**: Все list-эндпоинты должны поддерживать `limit`/`offset` для больших наборов данных.
5. **Валидация**: Использовать zod-схемы для валидации входных параметров и ответов API.
